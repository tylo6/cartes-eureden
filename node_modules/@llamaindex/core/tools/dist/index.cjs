Object.defineProperty(exports, '__esModule', { value: true });

var env = require('@llamaindex/env');
var index_cjs = require('../../zod/dist/index.cjs');

class FunctionTool {
    #fn;
    #additionalArg;
    #metadata;
    #zodType;
    #logger;
    constructor(fn, metadata, zodType, additionalArg, logger){
        this.#zodType = null;
        this.bind = (additionalArg)=>{
            return new FunctionTool(this.#fn, this.#metadata, this.#zodType ?? undefined, additionalArg);
        };
        this.call = (input)=>{
            let params = input;
            if (this.#zodType) {
                const result = index_cjs.safeParseSchema(this.#zodType, input);
                if (result.success) {
                    params = result.data;
                } else {
                    this.#logger.warn(result.error);
                }
            }
            return this.#fn.call(null, params, this.#additionalArg);
        };
        this.#fn = fn;
        this.#metadata = metadata;
        if (zodType) {
            this.#zodType = zodType;
        }
        this.#additionalArg = additionalArg;
        this.#logger = logger ?? env.consoleLogger;
    }
    // ----------------- IMPLEMENTATION -----------------
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    static from(fnOrConfig, schema) {
        if (typeof schema === "undefined" && typeof fnOrConfig === "object" && fnOrConfig.execute) {
            const { execute, parameters, ...restConfig } = fnOrConfig;
            if (index_cjs.isZodSchema(parameters)) {
                const jsonSchema = index_cjs.zodToJsonSchema(parameters);
                return new FunctionTool(execute, {
                    ...restConfig,
                    parameters: jsonSchema
                }, parameters);
            }
            return new FunctionTool(execute, fnOrConfig);
        }
        if (schema && index_cjs.isZodSchema(schema.parameters)) {
            const jsonSchema = index_cjs.zodToJsonSchema(schema.parameters);
            return new FunctionTool(fnOrConfig, {
                ...schema,
                parameters: jsonSchema
            }, schema.parameters);
        }
        return new FunctionTool(fnOrConfig, schema);
    }
    get metadata() {
        return this.#metadata;
    }
}
/**
 * A simpler alias for creating a FunctionTool.
 */ const tool = FunctionTool.from;

exports.FunctionTool = FunctionTool;
exports.tool = tool;
